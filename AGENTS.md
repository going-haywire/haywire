# Agent instructions

Project instructions live in [CLAUDE.md](CLAUDE.md) — read it and follow it.
Everything there applies to every coding agent, not just Claude: architecture
constraints, docs-first lookup order, testing and quality gates, and the
`.insights/` trap list.
