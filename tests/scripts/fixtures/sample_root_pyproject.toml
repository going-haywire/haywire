[project]
name = "fake-workspace"
version = "0.0.0"

[tool.uv.workspace]
members = [
    "subdir-a/*",
    "subdir-b/*",
]

[tool.haywire.release]
pip_publish_order = [
    "alpha-pkg",
    "beta-pkg",
]
git_publish_order = [
    "delta-pkg",
]
lockstep_unpublished = [
    "gamma-pkg",
]
