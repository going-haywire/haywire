 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |---
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |status: draft
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |doc_template: canonical-example
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |scope: The haybale-marketplace plugin — the optional library installer/browser haybale, its editors, its manager + marketplace states, and the optionality contract that lets the studio run without it
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |see-also:
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |  - haybale-marketplace-arch.md
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |  - ../library-canon.md
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |  - ../haybale-package-canon.md
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |  - ../../architecture/library-system/library-system-arch.md
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |  - ../../adr/0001-haybale-marketplace-carveout.md
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |  - ../../reference/glossary.md
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |---
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |# Library Marketplace — Canonical Example
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |## 1. What it solves
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |**`haybale-marketplace`** is the optional haybale package that gives the studio its in-app *library manager*: the surface where a user browses, installs, enables, disables, inspects, and uninstalls haywire libraries. It is the consumer-facing arm of the marketstall runtime (`haywire.core.marketstall`) — the runtime resolves catalogs; this plugin renders them and drives `uv pip install`.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |It is the **Library Manager** of the glossary's "five meanings of library" (meaning **#4**, see [reference/glossary §Library — five distinct meanings](../../reference/glossary.md#library-five-distinct-meanings)). It is *not* the Library System (meaning #2, the runtime that loads `Library` classes) and *not* a `Library` itself in the authoring sense (meaning #1) — though it ships *as* a haybale package and registers like any other plugin.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |The plugin was carved out of `haybale-studio` per [ADR-0001](../../adr/0001-haybale-marketplace-carveout.md). The load-bearing property is **optionality**: a derivative project can ship a Haywire build with no marketplace UI, or swap in a different installer, without touching the studio runtime. `haywire init` installs it by default, so the default user experience is unchanged.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |## 2. How it fits
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |```text
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |haywire.core.marketstall          haybale-marketplace             haybale-studio
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |────────────────────────          ───────────────────             ──────────────
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |the runtime:                      the plugin (this doc):          the host studio:
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. | parse / refresh / resolve        - 4 editors (browser,            provides the editor
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. | marketplace + marketstall          overview, component,           slots (action/edit/context)
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. | files into a Haybale catalog       source) + 1 dialog module      into which the editors
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |                          ◀──────  - MarketplaceState              self-register.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |                                    - LibraryManager (+            If haybale-marketplace
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |                                      LibraryManagerState)         is absent, the ACTION slot
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |                                  - drives uv pip install          is simply empty — no
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |                                                                   defensive code needed.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |       LibraryRegistry  ◀──────── manager.registry.enable/
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |       (core) owns                 disable_library(id);
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |       enable/disable              registry persists via
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |       persistence via HostStore   host.toml
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |```
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |The plugin owns **no registry state and no runtime parsing**. It calls `MarketplaceState` for catalog data (which wraps `haywire.core.marketstall`) and `LibraryManager` for install verbs (which shells out to `uv` and asks the Library System to rescan). Enable/disable persistence belongs to the core `LibraryRegistry`, not the plugin.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |**Boundaries.** *How* the editors, refresh pipeline, two-tier marketplace files, conflict resolution, and `InstallType`-gated actions work — see [haybale-marketplace-arch](haybale-marketplace-arch.md). *Why* the marketplace/marketstall trust model is shaped the way it is — see [sharing-arch](../../architecture/sharing/sharing-arch.md). What a `BaseLibrary`/`@library` author writes — see [library-canon](../library-canon.md). How a haybale is packaged and published — see [haybale-package-canon](../haybale-package-canon.md).
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |## 3. What the plugin ships
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |`barn/haybale-marketplace/haybale_marketplace/` registers two component categories from its `Library.register_components()`: states (via `LibraryStateRegistry`) and editors (via `EditorTypeRegistry`). **State is scanned before editors** — editor modules transitively import the state classes, and scanning them first keeps a single class object live (the same ordering rule `haybale-studio` follows).
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |### Editors
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Editor | Default slot | Role |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. ||---|---|---|
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `LibraryBrowserEditor` | `left` | Lists installed + available libraries, grouped REQUIRED / ENABLED / DISABLED / AVAILABLE. Burger menu: Refresh, Add Source, Share Project, Edit File. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `LibraryOverviewEditor` | `main` | One library's identity, component breakdown, and Edit / Enable / Disable / Uninstall / Install actions. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `LibraryComponentEditor` | `right` | Detail view for one component (node/type/widget/…) — import snippet, port-wiring hints. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |`library_marketplace_dialog` is **not** a registered editor — it is a module of helper functions (`show_add_source_dialog`, conflict-resolution prompts) the Browser's Add Source button calls.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |### Publishing a project
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |`haywire share` publishes the whole project: every `barn/*` library is bumped to
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |the same version (lockstep), docs are regenerated, `marketstall.toml` is rebuilt,
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |and the result is committed, tagged `v<version>`, and pushed.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Mode | What it does |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || --- | --- |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `haywire share` | Interactive. Prompts through the same six steps as the GUI wizard. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `haywire share --bump patch` | Non-interactive. Every answer comes from a flag; refuses to run with unresolved dependency drift. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `haywire deps check` | Read-only. Checks every `barn/*` library's dependency manifests for drift, exits 1 if any library has drift, exits 0 otherwise. Never writes. Use it as a PR gate. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |The same pipeline backs the **Share Project…** item in the Marketplace editor's
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |burger menu. The share URL is always branch-live — it tracks whatever branch
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |you published from, because `marketstall.toml` is a subscription feed and a
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |frozen, tag-pinned URL would lock subscribers to whatever version they first
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |subscribed to.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |### States
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || State | Kind | Owns |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. ||---|---|---|
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `MarketplaceState` | `AppState` | Marketplace orchestration for one session — wraps `haywire.core.marketstall` (`get_global()`, `get_project_haybales()`, `refresh()`, `remove_stale_haybale()`). The UI never calls marketstall functions directly. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `LibraryManagerState` | `AppState` | A thin holder publishing the `LibraryManager` so other editors reach it via `ctx.app_data[LibraryManagerState].manager.X`. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |### The `LibraryManager` service
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |`LibraryManager` (`library_manager.py`) is a **plain class, not an `AppState`** — it owns the install / uninstall / enable / disable / edit-identity verbs. Composition over inheritance: its constructor takes `(registry, project_dir)`, which an `AppState`'s bare `cls()` instantiation can't supply, so `LibraryManagerState` resolves those from the DI context in `on_enable()` and holds the manager. See [ADR-0001 §Why composition](../../adr/0001-haybale-marketplace-carveout.md).
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |## 4. Important concepts
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |**Optionality is the whole point.** Editors self-register into slots via `EditorTypeRegistry`. If `haybale-marketplace` is not installed, the left-slot library browser simply doesn't appear; `haybale-studio` carries no `if marketplace_present:` branches. This is what makes the carve-out worth its one extra `.manager.` indirection — a build can omit or replace the installer cleanly.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |**Persistence lives in the core registry, not here.** "Which libraries are disabled" is a property of the `LibraryRegistry`, not the installer. The editors call `manager.registry.enable_library(id)` / `disable_library(id)`; the registry writes through to `HostStore` (`<workspace>/.haywire/host.toml`, `[libraries] disabled`). There is no marketplace-owned persistence and no `AppState` in the enable/disable path. See [ADR-0001 §Why persistence moves out](../../adr/0001-haybale-marketplace-carveout.md).
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |**It depends on `haybale-studio`, not the reverse.** `haybale-marketplace`'s `pyproject.toml` declares `haywire-core`, `haywire-studio`, and `haybale-studio` as dependencies — it consumes the studio's slots and editor base classes. `haybale-studio` declares no dependency on the marketplace; the relationship is strictly one-directional, which is what preserves optionality.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |**`file_watcher=True`.** The plugin enables hot-reload like any editable haybale — edit an editor and the studio re-renders without restart (editable install only).
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |**Entry point.**
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |```toml
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |[project.entry-points."haywire.libraries"]
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |marketplace = "haybale_marketplace:Library"
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |```
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |## 5. Live example from the codebase
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |Source: [`barn/haybale-marketplace/haybale_marketplace/__init__.py`](../../../barn/haybale-marketplace/haybale_marketplace/__init__.py)
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |```python
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |@library(
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |    label="Library Marketplace",
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |    id="marketplace",
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |    version=_pkg_version("haybale-marketplace"),
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |    description="Library installer + browser editors",
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |    dependencies=[],
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |    tags=["marketplace"],
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |    file_watcher=True,
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |)
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |class Library(BaseLibrary):
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |    def register_components(self):
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |        base_path = Path(__file__).parent
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |        # state/ MUST be scanned before editors/. Editor modules transitively
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |        # import classes from state/ (LibraryManagerState, MarketplaceState);
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |        # scanning state first keeps a single class object live.
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |        self.add_folder_to_registry(
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |            folder_path=str(base_path / "state"),
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |            registry_cls=LibraryStateRegistry,
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |        )
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |        self.add_folder_to_registry(
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |            folder_path=str(base_path / "editors"),
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |            registry_cls=EditorTypeRegistry,
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |        )
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |    def validate(self) -> bool:
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |        return True
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |```
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |What this example exercises:
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Concept | Where |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. ||---|---|
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || A standalone optional plugin registering studio UI | the whole package |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `version` sourced from `importlib.metadata.version(...)` — pyproject is the source of truth | `version=_pkg_version("haybale-marketplace")` |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Two registry categories: states + editors | two `add_folder_to_registry` calls |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || State-before-editors scan ordering | comment in `register_components` |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Hot-reload for an editable plugin | `file_watcher=True` |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `dependencies=[]` on the `@library` decorator (studio deps are in `pyproject.toml`, not here) | decoration |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |---
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |## Quick reference
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |### What lives where
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Thing | Path |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. ||---|---|
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `Library` (entry point) | `barn/haybale-marketplace/haybale_marketplace/__init__.py` |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `LibraryManager` service | `…/library_manager.py` |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || `MarketplaceState`, `LibraryManagerState` | `…/state/` |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Browser / Overview / Component / Source editors | `…/editors/` |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Add-Source + conflict dialogs (helper module) | `…/editors/library_marketplace_dialog.py` |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |### Reaching the manager from another editor
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |```python
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |state = ctx.app_data[LibraryManagerState]
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |state.manager.install(entry)            # uv pip install
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |state.manager.registry.disable_library(lib_id)   # registry persists via host.toml
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |```
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |### Common pitfalls
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Pitfall | Why it matters |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. ||---|---|
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Adding a `haybale-marketplace` dependency from `haybale-studio` | Breaks optionality — the dependency must stay one-directional |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Making `LibraryManager` inherit `AppState` | Constructor-shape mismatch; the plugin publishes it via `LibraryManagerState` instead |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Persisting disabled-state in the plugin | Persistence belongs to the core `LibraryRegistry` / `HostStore`, not the marketplace |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. || Scanning `editors/` before `state/` | Editor modules import state classes; wrong order leaves stale class objects |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |
 Non-interactive — the only CLI mode. Every answer comes from a flag or takes its inert default; an undeclared import is DECLARED for you (unambiguously correct), while removals and floor changes are left alone. |For *how* the surface is built (refresh pipeline, two-tier files, conflict resolution, install gating), see [haybale-marketplace-arch](haybale-marketplace-arch.md). For the architectural rationale of the carve-out, see [ADR-0001](../../adr/0001-haybale-marketplace-carveout.md).
