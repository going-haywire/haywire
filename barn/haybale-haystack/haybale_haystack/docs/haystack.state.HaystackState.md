# Haystack State

`haystack:state:HaystackState` · kind: state

## Notes

In-memory registry of open graphs (one entry per file path)
Validation broadcast goes directly via``SessionManager``.
Haystack file load/dump live in ``persistence.py``
