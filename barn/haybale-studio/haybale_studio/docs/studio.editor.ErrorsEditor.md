# Errors

`studio:editor:ErrorsEditor` · kind: editor

Error ledger. Lists HaywireExceptions logged since startup; click one for details.

## Details

- **default_slot**: `info`
- **opens**: `OpenBehavior.REQUIRED`
- **order**: `100`

## Notes

Renders the process-wide error ledger as a clickable list with a detail pane.
